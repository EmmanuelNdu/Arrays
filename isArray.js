// isArray() checks if the first value is an array

const names = ['Florin', 'Ivan', 'Liam'];
const str = 'Hello World';
const number = 17;
console.log(Array.isArray(number)); // This will be false because number is not an array