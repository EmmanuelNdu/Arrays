const names = ['Florin', 'Ivan','Liam', 'Jam'];

const res = names.includes('Ivan');

console.log(res);

const fruits = ['orange', 'mango', 'Apple', 'watermelon'];
const pes = fruits.includes('mango');

if (pes) {
    console.log('Yuppy!')
} else {
    console.log('sadly... let us  buy an orange');
}